<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Links</title>
</head>
<body>
    <%for (let i = 0; i < links.length; i++) {%>
        <div id="<%=links[i]._id%>">
            <h2><%=links[i].title%></h2>
            <div><%=links[i].description%></div>
            <div><%=links[i].url%></div>
            <button onclick="deleteLink('<%=links[i]._id%>')">Deletar Link</button>
        </div>
    <%}%>

    <script>
        function deleteLink(id) {
            console.log(id)
            fetch(`http://localhost:8080/del/${id}`).then(res => res.json()).then(doc => console.log(doc))
        }
    </script>
</body>
</html>